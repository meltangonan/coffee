{
  "name": "coffee-journal",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight local-first coffee journal for home espresso.",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
